{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "start": "lerna clean -y && lerna bootstrap && lerna run start --parallel"
  }
}
